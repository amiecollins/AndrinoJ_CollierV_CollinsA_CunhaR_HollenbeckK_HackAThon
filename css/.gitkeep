const config = {
    uname: "root",
    upass: "root", // windows users leave blank!!
    port: 8889, // 3306 for windows or linux
    host: "localhost",
    db: "bizcard"
}

module.exports = config;